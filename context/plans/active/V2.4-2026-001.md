# 📋 v2.4.0 开发计划 - 预测模型优化

**制定时间:** 2026-03-27 12:40 UTC  
**修订原因:** 回归 GitHub 提交 (3de51c2) 原始规划  
**当前版本:** v2.3.0 (最新)  
**目标版本:** v2.4.0  
**预计周期:** 2026-04-01 ~ 2026-04-30 (4 周)  
**预计工作量:** ~60 小时

---

## 🎯 v2.4.0 目标

**主题:** 🤖 预测模型优化

**核心目标:**
1. Prophet 时间序列预测
2. LSTM 深度学习预测
3. 预测准确率评估
4. 多模型对比分析
5. 预测结果可视化

---

## 📊 现状分析

### v2.3.0 已有功能
- ✅ 线性回归预测 (30 天)
- ✅ 季节性因子调整
- ✅ 置信区间计算
- ✅ 6 维度评估体系

### 待改进项
| 问题 | 影响 | 优先级 |
|------|------|--------|
| 线性模型简单 | 复杂模式识别不足 | 🔴 高 |
| 无深度学习 | 长期依赖捕捉弱 | 🔴 高 |
| 准确率未验证 | 可信度待提升 | 🟡 中 |
| 无多模型对比 | 选型困难 | 🟡 中 |

---

## 📅 开发阶段

### Phase 1: Prophet 集成 (Week 1: 04-01 ~ 04-07)

**目标:** Facebook Prophet 时间序列预测

**任务清单:**

**1.1 安装依赖**
```bash
pip install prophet>=1.1.5
pip install holidays  # 节假日支持
```

**1.2 Prophet 预测模块**
创建 `src/analysis/prophet_predictor.py`:
```python
from prophet import Prophet
import pandas as pd

class ProphetPredictor:
    def __init__(self, 
                 yearly_seasonality: bool = True,
                 weekly_seasonality: bool = True,
                 daily_seasonality: bool = False,
                 holidays: pd.DataFrame = None):
        """初始化 Prophet 模型"""
        
    def prepare_data(self, df: pd.DataFrame) -> pd.DataFrame:
        """准备数据 (ds, y 格式)"""
        
    def train(self, df: pd.DataFrame) -> None:
        """训练模型"""
        
    def predict(self, periods: int = 30) -> pd.DataFrame:
        """预测未来 N 天"""
        
    def add_changepoints(self, n_changepoints: int = 25) -> None:
        """添加变点检测"""
        
    def add_regressors(self, regressors: list) -> None:
        """添加回归因子 (价格/促销等)"""
        
    def cross_validation(self, horizon: str = '30 days') -> pd.DataFrame:
        """交叉验证"""
        
    def get_metrics(self) -> dict:
        """获取评估指标 (MAPE/RMSE/MAE)"""
```

**1.3 节假日支持**
创建 `src/analysis/holidays.py`:
```python
# 美国节假日
US_HOLIDAYS = pd.DataFrame({
    'holiday': ['New Year', 'Thanksgiving', 'Christmas'],
    'ds': pd.to_datetime(['2026-01-01', '2026-11-26', '2026-12-25']),
    'lower_window': 0,
    'upper_window': 1,
})

# 中国节假日
CN_HOLIDAYS = pd.DataFrame({
    'holiday': ['Spring Festival', 'National Day'],
    'ds': pd.to_datetime(['2026-02-17', '2026-10-01']),
    'lower_window': 0,
    'upper_window': 7,
})

# 购物节
SHOPPING_FESTIVALS = pd.DataFrame({
    'holiday': ['Prime Day', 'Black Friday', 'Cyber Monday', 'Double 11'],
    'ds': pd.to_datetime(['2026-07-15', '2026-11-28', '2026-12-01', '2026-11-11']),
    'lower_window': -7,
    'upper_window': 7,
})
```

**1.4 可视化**
创建 `src/analysis/prophet_visualizer.py`:
```python
def plot_forecast(model, forecast, save_path: str = None):
    """绘制预测图"""
    
def plot_components(model, forecast, save_path: str = None):
    """绘制组件图 (趋势/季节性/节假日)"""
    
def plot_changepoints(model, save_path: str = None):
    """绘制变点图"""
```

**1.5 测试**
创建 `tests/test_prophet_predictor.py`:
- 数据准备测试
- 模型训练测试
- 预测测试
- 交叉验证测试
- 评估指标测试

**交付物:**
- `src/analysis/prophet_predictor.py`
- `src/analysis/holidays.py`
- `src/analysis/prophet_visualizer.py`
- `tests/test_prophet_predictor.py`
- 完成报告

**预计工时:** 15 小时

---

### Phase 2: LSTM 深度学习 (Week 2: 04-08 ~ 04-14)

**目标:** LSTM 神经网络预测

**任务清单:**

**2.1 安装依赖**
```bash
pip install tensorflow>=2.15.0
pip install keras>=2.15.0
pip install scikit-learn>=1.3.0
```

**2.2 LSTM 预测模块**
创建 `src/analysis/lstm_predictor.py`:
```python
import tensorflow as tf
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import LSTM, Dense, Dropout
from sklearn.preprocessing import MinMaxScaler

class LSTMPredictor:
    def __init__(self, 
                 lookback: int = 60,
                 forecast_horizon: int = 30,
                 lstm_units: list = [50, 25],
                 dropout: float = 0.2):
        """初始化 LSTM 模型"""
        
    def prepare_sequences(self, data: np.ndarray) -> tuple:
        """准备序列数据 (X, y)"""
        
    def build_model(self, input_shape: tuple) -> tf.keras.Model:
        """构建 LSTM 模型"""
        
    def train(self, X_train: np.ndarray, y_train: np.ndarray,
              X_val: np.ndarray, y_val: np.ndarray,
              epochs: int = 100, batch_size: int = 32) -> tf.keras.callbacks.History:
        """训练模型"""
        
    def predict(self, X: np.ndarray) -> np.ndarray:
        """预测"""
        
    def inverse_transform(self, predictions: np.ndarray) -> np.ndarray:
        """反归一化"""
        
    def evaluate(self, y_true: np.ndarray, y_pred: np.ndarray) -> dict:
        """评估 (MAPE/RMSE/MAE)"""
        
    def save_model(self, path: str) -> None:
        """保存模型"""
        
    def load_model(self, path: str) -> None:
        """加载模型"""
```

**2.3 特征工程**
创建 `src/analysis/lstm_features.py`:
```python
def create_features(df: pd.DataFrame) -> pd.DataFrame:
    """创建特征:
    - 滞后特征 (lag_1, lag_7, lag_30)
    - 滚动统计 (rolling_mean_7, rolling_std_30)
    - 时间特征 (month, day_of_week, is_weekend)
    - 价格特征 (price_change_pct)
    """
    
def create_target(df: pd.DataFrame, horizon: int = 30) -> pd.Series:
    """创建目标变量 (未来 N 天平均销量)"""
```

**2.4 超参数调优**
创建 `src/analysis/lstm_tuning.py`:
```python
from keras_tuner import HyperModel

class LSTMHyperModel(HyperModel):
    def build(self, hp):
        model = Sequential()
        model.add(LSTM(
            hp.Int('lstm_units', 32, 128, step=32),
            return_sequences=True,
            input_shape=(lookback, n_features)
        ))
        model.add(Dropout(hp.Float('dropout', 0.1, 0.5, step=0.1)))
        model.add(LSTM(hp.Int('lstm_units_2', 16, 64, step=16)))
        model.add(Dense(1))
        model.compile(optimizer='adam', loss='mse')
        return model
```

**2.5 可视化**
创建 `src/analysis/lstm_visualizer.py`:
```python
def plot_training_history(history, save_path: str = None):
    """绘制训练历史"""
    
def plot_predictions(y_true, y_pred, save_path: str = None):
    """绘制预测对比"""
    
def plot_residuals(y_true, y_pred, save_path: str = None):
    """绘制残差图"""
```

**2.6 测试**
创建 `tests/test_lstm_predictor.py`:
- 序列准备测试
- 模型构建测试
- 训练测试
- 预测测试
- 评估测试

**交付物:**
- `src/analysis/lstm_predictor.py`
- `src/analysis/lstm_features.py`
- `src/analysis/lstm_tuning.py`
- `src/analysis/lstm_visualizer.py`
- `tests/test_lstm_predictor.py`
- 完成报告

**预计工时:** 20 小时

---

### Phase 3: 模型对比与评估 (Week 3: 04-15 ~ 04-21)

**目标:** 多模型对比分析

**任务清单:**

**3.1 统一评估框架**
创建 `src/analysis/model_evaluator.py`:
```python
class ModelEvaluator:
    def __init__(self, models: dict):
        """初始化多个模型"""
        
    def train_all(self, train_data: pd.DataFrame) -> None:
        """训练所有模型"""
        
    def predict_all(self, test_data: pd.DataFrame) -> dict:
        """所有模型预测"""
        
    def evaluate_all(self, y_true: np.ndarray) -> pd.DataFrame:
        """评估所有模型:
        - MAPE (平均绝对百分比误差)
        - RMSE (均方根误差)
        - MAE (平均绝对误差)
        - R² (决定系数)
        """
        
    def plot_comparison(self, save_path: str = None):
        """绘制模型对比图"""
        
    def get_best_model(self, metric: str = 'MAPE') -> str:
        """获取最佳模型"""
```

**3.2 模型对比报告**
创建 `src/analysis/model_comparison.py`:
```python
def generate_comparison_report(evaluator: ModelEvaluator) -> dict:
    """生成对比报告:
    - 各模型指标对比
    - 预测时间对比
    - 训练时间对比
    - 最佳模型推荐
    """
    
def export_report(report: dict, path: str) -> None:
    """导出报告 (JSON/Markdown)"""
```

**3.3 可视化对比**
创建 `web/templates/analysis/model_comparison.html`:
- 多模型预测对比图
- 误差分布对比
- 训练时间对比
- 最佳模型推荐

**3.4 测试**
创建 `tests/test_model_evaluator.py`:
- 评估框架测试
- 对比报告测试
- 可视化测试

**交付物:**
- `src/analysis/model_evaluator.py`
- `src/analysis/model_comparison.py`
- `web/templates/analysis/model_comparison.html`
- `tests/test_model_evaluator.py`
- 完成报告

**预计工时:** 15 小时

---

### Phase 4: 集成与优化 (Week 4: 04-22 ~ 04-30)

**目标:** 模型集成与性能优化

**任务清单:**

**4.1 模型集成**
创建 `src/analysis/ensemble_predictor.py`:
```python
class EnsemblePredictor:
    def __init__(self, models: dict, weights: dict = None):
        """初始化集成模型"""
        
    def predict(self, X: np.ndarray) -> np.ndarray:
        """加权平均预测"""
        
    def predict_stacking(self, X: np.ndarray) -> np.ndarray:
        """Stacking 集成"""
        
    def get_optimal_weights(self, validation_data: pd.DataFrame) -> dict:
        """优化权重"""
```

**4.2 自动化预测管道**
创建 `src/analysis/predict_pipeline.py`:
```python
class PredictionPipeline:
    def __init__(self, config: dict):
        """初始化预测管道"""
        
    def run(self, asin: str) -> dict:
        """运行完整预测流程:
        1. 加载历史数据
        2. 数据预处理
        3. 多模型预测
        4. 模型选择/集成
        5. 结果可视化
        """
        
    def save_results(self, results: dict, path: str) -> None:
        """保存结果"""
```

**4.3 API 端点**
更新 `web/routes/analysis.py`:
```python
GET /api/v2/analysis/predict/<asin>           # 单商品预测
POST /api/v2/analysis/predict/batch           # 批量预测
GET /api/v2/analysis/compare-models           # 模型对比
GET /api/v2/analysis/forecast/<asin>/chart    # 预测图表
```

**4.4 性能优化**
- 模型缓存 (避免重复训练)
- 并行预测 (多商品同时预测)
- 增量训练 (新数据增量更新)

**4.5 文档更新**
更新 `docs/02-User-Guide/prediction.md`:
- Prophet 使用说明
- LSTM 使用说明
- 模型对比指南
- 预测准确率说明

**交付物:**
- `src/analysis/ensemble_predictor.py`
- `src/analysis/predict_pipeline.py`
- 更新后的 API 端点
- 更新后的文档
- 完成报告

**预计工时:** 10 小时

---

## 📊 总计

| Phase | 内容 | 工时 | 交付物 |
|-------|------|------|--------|
| Phase 1 | Prophet 集成 | 15h | 3 模块 + 测试 |
| Phase 2 | LSTM 深度学习 | 20h | 5 模块 + 测试 |
| Phase 3 | 模型对比 | 15h | 3 模块 + UI + 测试 |
| Phase 4 | 集成优化 | 10h | 2 模块 + API + 文档 |
| **总计** | | **60h** | **13 模块 + 4 测试** |

---

## 🎯 成功指标

**模型性能:**
- [ ] Prophet MAPE < 15%
- [ ] LSTM MAPE < 12%
- [ ] 集成模型 MAPE < 10%
- [ ] 优于线性基线 > 20%

**功能完整性:**
- [ ] Prophet 预测可用
- [ ] LSTM 预测可用
- [ ] 模型对比可用
- [ ] 集成预测可用

**用户体验:**
- [ ] 预测结果可视化
- [ ] 模型对比图表
- [ ] 预测准确率显示

---

## 📅 里程碑

| 里程碑 | 日期 | 内容 |
|--------|------|------|
| M1 | 04-07 | Prophet 集成完成 |
| M2 | 04-14 | LSTM 模型完成 |
| M3 | 04-21 | 模型对比完成 |
| M4 | 04-30 | v2.4.0 发布 |

---

## 🔗 依赖关系

**v2.3.0 已完成:**
- ✅ 数据采集
- ✅ 线性回归预测 (基线)
- ✅ 6 维度评估
- ✅ 可视化模块

**v2.4.0 依赖:**
- Prophet → pandas, numpy
- LSTM → tensorflow, scikit-learn
- 模型对比 → 所有预测模块

---

## 💡 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| LSTM 训练时间长 | 中 | 使用 GPU/减少 epochs |
| 过拟合 | 中 | Dropout/Early Stopping |
| 数据量不足 | 高 | 数据增强/迁移学习 |
| Prophet 安装问题 | 低 | 使用 Docker/预构建镜像 |

---

## 📈 预期提升

| 指标 | v2.3.0 (线性) | v2.4.0 (Prophet+LSTM) | 提升 |
|------|---------------|----------------------|------|
| MAPE | ~20% | <10% | 50%+ |
| 长期预测 | 弱 | 强 | 显著 |
| 季节性捕捉 | 中 | 强 | 显著 |
| 节假日影响 | 无 | 有 | 新增 |

---

**制定者:** @admina  
**审核状态:** 待审核  
**批准状态:** 待批准
